/**
 * Returns a percentage of val between min and max.
 * @param min
 * @param max
 * @param val
 * @returns {number}
 */
export declare function toPercent(min: number, max: number, val: number): number;
export declare function getCustomPropOrVal(val: string, backup?: string): string;
